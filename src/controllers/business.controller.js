export const getAllBusinesses = (req, res) => {
  res.send({ status: "sucees", result: "getAllBusinesses" });
};

export const createBusiness = (req, res) => {
  res.send({ status: "sucees", result: "createBusiness" });
};

export const getBusinessById = (req, res) => {
  res.send({ status: "sucees", result: "getBusinessById" });
};

export const addProduct = (req, res) => {
  res.send({ status: "sucees", result: "addProduct" });
};
