export const getOrders = (req, res) => {
  res.send({ status: "sucees", result: "orders" });
};

export const createOrder = (req, res) => {
  res.send({ status: "sucees", result: "createOrder" });
};

export const getOrderById = (req, res) => {
  res.send({ status: "sucees", result: "getOrderById" });
};

export const resolveOrder = (req, res) => {
  res.send({ status: "sucees", result: "addProduct" });
};
